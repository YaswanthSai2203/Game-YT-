# Optional — only if using a separate leaderboard server (not needed on Vercel)
# VITE_LEADERBOARD_API_URL=https://neon-pulse-leaderboard.your-name.workers.dev
#
# On Vercel: connect KV storage instead — see docs/VERCEL_LEADERBOARD.md
